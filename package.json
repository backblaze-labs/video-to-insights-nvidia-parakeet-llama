{
  "name": "video-to-insights-pipeline",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/backblaze-labs/video-to-insights-nvidia-parakeet-llama.git"
  },
  "homepage": "https://github.com/backblaze-labs/video-to-insights-nvidia-parakeet-llama#readme",
  "bugs": {
    "url": "https://github.com/backblaze-labs/video-to-insights-nvidia-parakeet-llama/issues"
  },
  "license": "MIT",
  "scripts": {
    "doctor": "node scripts/doctor.mjs",
    "predev": "node scripts/doctor.mjs",
    "dev": "sh scripts/dev.sh",
    "dev:web": "pnpm --filter @video-to-insights-pipeline/web dev",
    "dev:api": "cd services/api && .venv/bin/uvicorn main:app --reload --port ${API_PORT:-8000} --workers 1",
    "build": "pnpm --filter @video-to-insights-pipeline/web build",
    "typecheck": "pnpm --filter @video-to-insights-pipeline/web typecheck",
    "lint": "pnpm --filter @video-to-insights-pipeline/web lint",
    "lint:api": "cd services/api && .venv/bin/ruff check .",
    "test:api": "cd services/api && .venv/bin/python -m pytest",
    "check:structure": "cd services/api && .venv/bin/python -m pytest tests/test_structure.py -v"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
